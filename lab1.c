#include <stdio.h>
void main()
{
	int a,b,diff=0,sum=0,mul=0,div=0;
	printf("enter two numbers ");
	scanf("%d %d", &a,&b);
	 diff=a-b;
	   printf(" the difference of two nunbers is: %d \n" , diff);
	  
	sum=a+b;
	   printf("the sum is %d\n" , sum);
	  mul=a*b;
	   printf("the product is %d\n", mul);
	   div=a/b;
	   printf( "the quotient is %d" ,div);
	   
	
	
}
